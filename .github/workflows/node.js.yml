name: Platform Engineer CI

on:
  push:
    branches: ["main"]

jobs:
  platform_check:
    runs-on: ubuntu-latest

    steps:
      - name: Download project code
        uses: actions/checkout@v3

      - name: Install Python on cloud computer
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: Check if SQL file exists
        run: echo "SQL is there! Good job! 🟢"

      - name: Run Platform Check Script
        run: python platform-check.py
